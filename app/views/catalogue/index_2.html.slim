ruby:
  menu_items = [
    #{
    #  name: "featured",
    #  text: "подборки",
    #  special: "featured"
    #},
    {
      name: "mono",
      text: "классический",
      selected: true
    },
    {
      name: "aroma",
      text: "ароматизированный"
    },
    {
      name: "exotic",
      text: "редкий и экзотический"
    },
    {
      name: "espresso",
      text: "для кофемашин"
    },
    {
      name: "all",
      text: "весь кофе"
    }
  ]

/= render partial: 'application/header_2'

.bm-page-catalogue role="bm-page-catalogue"
  .bm-page-catalogue-content
    /= render partial: "catalogue/popular"

    .bm-page-catalogue-menu role="bm-page-catalogue-menu"
      - menu_items.each do |item|
        .bm-page-catalogue-menu-item role="bm-page-catalogue-menu-item" data-name=item[:name] class="#{'m-selected' if item[:selected].present?}" data-special="#{item[:special] if item[:special].present?}"
          .bm-page-catalogue-menu-item-image
          .bm-page-catalogue-menu-item-text
            = item[:text]

    .bm-page-catalogue-items-wrapper role="bm-catalogue-items-wrapper"
      /.bm-page-catalogue-items-special.m-featured role="bm-catalogue-items-special"
        - @items_groups_for_featured.each do |group|
          = render partial: "catalogue/item_group", locals: {items: group[:items], header: group[:header]}
      .bm-page-catalogue-items-grid role="bm-catalogue-items-grid" data-selected-category="mono"
        - @items.each_with_index do |item, i|
          = render partial: 'catalogue/item', locals: {item: item, counter: i + 1}


= render partial: 'templates/all'